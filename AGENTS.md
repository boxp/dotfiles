# AGENTS Instructions

- ユーザーには日本語で受け答えすること
- dotfiles リポジトリに変更を入れた場合は、ユーザーが明示的に止めない限り、作業完了前に必ず `master` ブランチへコミットまたは merge して反映し、`origin/master` へ push すること
- リポジトリの clone・取得・一覧確認・ローカル管理は原則として `ghq` を使い、直接 `git clone` で任意ディレクトリへ置かないこと
- リポジトリに修正を入れる場合は、原則的に以下のskillsを使ってworktree上で作業すること
  - skills/codex-review
  - codex-review-file
  - multi-repo-dev
  - worktree-cleanup
  - worktree-create
  - worktree-dev
